const {APIError} = require('../utils/error');
const APIFeatures = require('../utils/apiFeatures');

const logger = require('../config/logger');

const uploadMedias = async (files) => {
    logger.info('Uploading medias');

    

    return files;
}